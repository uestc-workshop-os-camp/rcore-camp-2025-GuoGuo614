* 本地测试能过而云端评分机器过不了，调了半天，又是改panic输出又是干啥。
  * 最后把测试用例拷下来一跑，原来是report没写啊.....
  * 妈妈我再也不偷懒了。
## 简单总结
- 本章的实现和文档讲授的知识感觉有点割裂，和分时多任务没什么关系。不过这个实验的目的应该是检查对源码的理解，搞明白到目前为止的文件结构和功能分配。
- 实验实现了一个新的系统调用，主要任务在查询当前任务系统调用的总次数。因此前面实现的系统调用需要加一个increase的函数，每次调用都让某个变量自增。
- 对于这个变量，因为每个任务的系统调用都是分开时，所以需要加在TaskControlBlock这个结构体里面。
- 有一个坑点记一下，想了一阵子用什么数据结构完成这个事情，哈希表因为是属于std库的东西没法直接用。想到尽量做好可扩展性，尝试了一下1024的大数组，但是会导致get_time出现问题，想想应该是内存占用太大，512貌似可以，不过还是老老实实用单个属性了。
  - 具体原因还需要去发掘。
- 之后查询就直接访问当前任务的times属性就可以啦。
- 关于这个系统调用读写本任务内存的功能，确实没感觉有什么用，而且给了个绝对地址，和当前任务是什么也毫无关系。应该是后面实现了进程和虚存之后就能看出作用了。
## 问答题
1. 程序报错如下：
[kernel] PageFault in application, bad addr = 0x0, bad instruction = 0x804003a4, kernel killed it.
[kernel] IllegalInstruction in application, kernel killed it.
[kernel] IllegalInstruction in application, kernel killed it.
  - 第一个程序是触发了页错误，也就是非法的内存访问。
  - 后两个程序，内核的表现是检测到了非法指令，故终止了应用。
2. 刚进入__restore时，sp指向的是内核栈，也就是由__alltraps 设置的，用户程序Trap上下文所在的地方，因此进行了寄存器的恢复。
  - 场景一：在Trap（系统调用、异常或中断）之后恢复到用户态，继续运行用户程序，故需要恢复Trap上下文。
  - 场景二：内核需要切换任务，因此调整sp指向另一个任务的内核栈，恢复上下文后即完成了任务切换。
3. sstatus寄存器：对于进入用户态的作用是恢复特权级相关的状态，确保返回用户态时环境与 Trap 发生前一致。比如设置U模式等。
  - spec寄存器：恢复了Trap之前的PC，决定了返回用户态之后从其哪一条指令开始执行。
  - sscratch寄存器：在Trap期间临时保存用户栈指针，在返回用户态后则重新设置为内核态指针，方便下次Trap时正确切换栈。
4. x2寄存器是sp，此时sp已经指向内核栈，完成上下文的恢复，不应该被破坏，在最后返回内核态时再从sscratch完成sp的恢复；关于x4也就是tp，不太了解，查了一下资料：
  - tp是线程指针
    - 在大多数应用场景下，用户程序不会用tp，或者tp由运行时环境（如线程库）管理，内核不需要关心它的值。
5. 上文已经回答过。
6. 发生在最后一条指令sret，gdb一波会发现，sret指令会根据sstatus的值和sepc的值来修改CPU的状态和PC，从而返回指定的地址继续执行用户程序。
```
# 下一步是 sret
(gdb) p/x $sstatus
$1 = 0x200000000
(gdb) p/x $sepc
$2 = 0x80400000
(gdb) p/x $pc
$3 = 0x8020154e
(gdb) si
0x0000000080400000 in ?? ()
(gdb) p/x $sstatus
$4 = 0x200000020
(gdb) p/x $pc
$5 = 0x80400000
```
7. 上文已经回答过。
8. 并不是trap.S里面的指令完成这件事，在__alltraps之前就发生了，是ecall这条指令。